package in.amankumar110.whatsappapp.enums;

import androidx.annotation.NonNull;

public enum SignupState {
    LOADING,   // Indicates that the signup process is in progress
    SUCCESS,   // Indicates that the signup process was successful
    ERROR      // Indicates that an error occurred during the signup process
}